import { DirectiveColorDirective } from './directive-color.directive';

describe('DirectiveColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveColorDirective();
    expect(directive).toBeTruthy();
  });
});
